<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bible notes 0.1 (beta)</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="" />
  <meta name="author" content="" />
  <%= stylesheet_link_tag    "bootstrap.min", "bootstrap-response.min", :media => "all" %>
  <%= javascript_include_tag "application", "bootstrap.min" %>
  <%= csrf_meta_tags %>
   <style type="text/css">
      body {
        padding-top: 60px;
        padding-bottom: 40px;
      }
    </style>
    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="assets/ico/apple-touch-icon-144-precomposed.png" />
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="assets/ico/apple-touch-icon-114-precomposed.png" />
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="assets/ico/apple-touch-icon-72-precomposed.png" />
    <link rel="apple-touch-icon-precomposed" href="assets/ico/apple-touch-icon-57-precomposed.png" />
</head>
<body>
<% if (user_signed_in?) %>
  <%= render :file => "common/_user_bar" %>
<% else %>
  <%= render :file => "common/_site_bar" %>
<% end %>
<div class="container">
<% if (notice!= nil) %>
  <%= notice %>
<% end %>

<% if (alert!= nil) %>
  <%= alert %>
<% end %>

<%= yield %>
</div>
</body>
</html>
